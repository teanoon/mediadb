.col-md-4.col-md-offset-4
  = simple_form_for [@medium, @article] do |f|
    = f.error_notification

    = f.input :title
    - if action_name != 'new'
      = f.input :url, input_html: { rows: 1 }
    = f.input :content, input_html: { rows: 10 }

    .form-actions
      = f.button :submit