.col-md-4.col-md-offset-4
  %h2= @article.title
  %p= link_to @article.url, @article.url
  %p
    = link_to t('phrase.approve'), '#', class: 'btn btn-default btn-xs'
    = link_to t('phrase.edit'), edit_article_path(@article), class: 'btn btn-default btn-xs'
    = link_to t('phrase.back'), articles_path, class: 'btn btn-default btn-xs'
  %hr
  = @article.content
